{
  "name": "discordbot",
  "version": "1.0.0",
  "description": "bot that runs through discord that will, upon command, download and run the most recent commit of a github repo. This was made because I didn't have access to the server computer but needed a way to launch my website remotely during development",
  "main": "bot.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Joshua Dietrich",
  "license": "ISC",
  "dependencies": {
    "discord.js": "^12.5.1"
  }
}
